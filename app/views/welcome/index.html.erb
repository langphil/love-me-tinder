<div class="row">
  <div class="col">
    <div id="tinder-container">
      <button class="accept_btn btn btn-primary">Start Dating</button>

      <div class="buddy" style="display: block;">
        <div class="avatar"  style="display: block; background-image: url(<%= @matches.first.profile_pic %>)">
        </div>
        <div class="avatar-name"
          <p><%= @matches.first.name %>, <%= @matches.first.age %></p>
        </div>
      </div>
      <div class="buddy"><div class="avatar" style="display: block; background-image: url(<%= @matches.find(2).profile_pic %>)"></div>
        <div class="avatar-name"
          <p><%= @matches.find(2).name %>, <%= @matches.find(2).age %></p>
        </div>
      </div>
      <div class="buddy"><div class="avatar" style="display: block; background-image: url(<%= @matches.find(3).profile_pic %>)"></div>
        <div class="avatar-name"
          <p><%= @matches.find(3).name %>, <%= @matches.find(3).age %></p>
        </div>
      </div>
      <div class="buddy"><div class="avatar" style="display: block; background-image: url(<%= @matches.find(4).profile_pic %>)"></div>
        <div class="avatar-name"
          <p><%= @matches.find(4).name %>, <%= @matches.find(4).age %></p>
        </div>
      </div>
      <div class="buddy"><div class="avatar" style="display: block; background-image: url(<%= @matches.find(5).profile_pic %>)"></div>
        <div class="avatar-name"
          <p><%= @matches.find(5).name %>, <%= @matches.find(5).age %></p>
        </div>
      </div>
      <div class="buddy"><div class="avatar" style="display: block; background-image: url(<%= @matches.find(6).profile_pic %>)"></div>
        <div class="avatar-name"
          <p><%= @matches.find(6).name %>, <%= @matches.find(6).age %></p>
        </div>
      </div>
    </div>

  </div>
</div>
